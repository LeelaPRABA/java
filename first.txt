cd d:
git init
git add 'file'
git commit -m "initial msg"
git remote add origin <code URL>
git push -u origin main




 commands will user for the code push to github
 
 
 
 
 
 cd d:
 git init
 git clone <code URL>
 
 
 
 these commands will used to pull the code from github
 
 
 
